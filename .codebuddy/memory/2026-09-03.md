# 2026-09-03

## 修复 mkdocs 文档站渲染问题（docs/ 目录）

背景：`docs/` 是 `mkdocs new docs` 生成的站点，素材由 Typora 编写（`docs/docs/*.md` + 同名 `.assets/` 图片目录）。

发现并修复的两类问题：

1. **代码块原样显示 ```bash**（共 243 处缩进围栏 + 3 处开闭围栏缩进不一致）
   - 根因：python-markdown 的 `fenced_code` 正则要求围栏顶格（`^`），缩进后不识别；
     `pymdownx.superfences` 可识别缩进围栏，但**同一列表项内正文与围栏缩进必须一致**，否则整体失效退化成普通缩进代码块。
   - 处理：`mkdocs.yml` 增加 `markdown_extensions: - pymdownx.superfences`；
     并按列表项结构统一缩进（正文行与围栏一起挪到 `target = max(4, 正文缩进, 围栏缩进)`，
     使代码块保留在列表项内、有序列表编号不中断）；非列表上下文的缩进围栏取消缩回到第 0 列。

2. **46 张图片 404**
   - 根因：这些图片用的是裸 HTML `<img src="./xxx.assets/a.png">`。mkdocs 只重写 markdown 语法
     （`![](...)`）的相对路径，裸 HTML 不动；而 `use_directory_urls` 让页面位于 `/<页面名>/` 下，
     `./` 解析到了错误目录。
   - 处理：全部转成 `![alt](src)`（顺带把 URL 编码的中文路径还原成真实中文文件名），
     原先的 `style="zoom:NN%"` 被去掉（readthedocs 主题有 `img{max-width:100%}`，图片不会溢出）。

验证方式（可复用）：`mkdocs build --strict` 无告警；生成页面里
`<pre><code>``` 数量为 0（未渲染的围栏）；按页面 URL 解析 `img src` 后 404 图片数为 0。

新增 `docs/requirements.txt`（mkdocs + pymdown-extensions，pymdown-extensions 为本次新增依赖）。

## 新增根目录 README.md（新手指南）

- 面向刚拿到仓库的新人，含 5 部分：环境配置（Python + .venv + requirements.txt）、
  项目预览（mkdocs serve/build/gh-deploy）、文件修改（内容改 docs/docs/*.md、结构改 docs/mkdocs.yml）、
  项目上传（git add/commit/push + 可选 gh-deploy）、问答补充（pymdown-extensions 作用 / gh-deploy 详解 /
  DOS与根目录疑问 / cmkdocs 手误）。
- 同时补充了 `.gitignore`（MkDocs/Python 规则：.venv/、site/、.site_check/、__pycache__ 等；保留文档图片不忽略）。

## 1 号文档加 mermaid 样板 + 渲染配置

- 需求：1-10 文档图片下加 mermaid 图。用户选择「先做 1 号样板（按每类图片）」+「改用 Material 主题」。
- **mermaid 渲染已配置**：`docs/mkdocs.yml` 的 `markdown_extensions` 增加 `pymdownx.superfences.custom_fences`（name=mermaid, class=mermaid, format=!!python/name:pymdownx.superfences.fence_code_format），并 `extra_javascript` 引入 `mermaid@10` CDN + 本地 `docs/docs/assets/javascripts/mermaid-init.js`。
  - 验证：`mkdocs build` 通过（0.53s），`!!python/name` 写法在 mkdocs 下有效。
  - **注意**：渲染发生在浏览器端（拉 CDN），构建本身不需要联网；离线构建 OK。
- **Material 主题未能安装**：沙箱执行 `pip install mkdocs-material` 时下载 9.3MB wheel 超时被杀（默认源/阿里源都超时，`pip.log` 显示 `Operation cancelled by user`）。故当前 `theme.name` 仍用已装好的 `readthedocs`，mermaid 配置两者通用。
  - 用户本地网络正常时：`pip install mkdocs-material` 后把 `theme.name` 改为 `material` 即可（mkdocs.yml 已留注释说明）。
- 1 号文档 `1走进Docker的世界.md` 已按“每类图片”代表性插入 **14 个 mermaid 图**（虚拟化对比、引擎 CS 架构、containerd 调用链、三大要素、Dockerfile 流程、CGroup、镜像分层、AUFS、bridge 网络、DNAT、container 模式、OCI/CRI、kubelet-cri、小结总览）。
  - 待用户确认风格/质量后再铺开其余 9 个文档、以及是否对 1 号其余图片也补全。

## 用户确认文字画风格 + 铺开到 2-10 号文档

- 用户看后反馈之前的 mermaid 图"都不太对"，要求：**全部去掉，重新画，最好画成原图一样；文字多的可用文字画**。
- 已先把 1 号文档的 14 个 mermaid 块用脚本全部移除（`rm_mermaid.py`），再改用 **ASCII「文字画」**（```text 代码块）按每类图片重画 14 张。
- 用户确认该风格后，将同样做法铺开到 2-10 号文档。本环境无法读取图片本身，故文字画均依据**图片所在章节的原文/标题**还原（对命令/界面截图还原成流程或框图，对架构图还原成拓扑）。
- 各文档插入数量：1→14、2→4、3→14、4→43、5→15、6→13、7→28、8→2、9→29、10→34（共约 196 处；少量同图多次出现合并为 1 处）。
- 实现方式：用 Python 脚本在图片所在行后插入 ```text 块。为避免中文长 URL 锚点截断，文件 4/7/9/10 改用「按图片文件名 token 定位行」插入。
- 全部完成后 `mkdocs build` 通过（0.59s），并清理了 ctx*.txt / count_imgs.py / add_*.py / rm_mermaid.py 等临时文件。
- 注意：本沙箱**看不到图片**，文字画是基于上下文的等价还原，并非像素级复刻原图；若某张图需更贴近原图，需用户贴图或描述图内结构。
